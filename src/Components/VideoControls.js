import React from "react";

const VideoControls = () => {
  return (
    <div className="absolute bottom-0 left-0 z-10 flex flex-col w-full px-5 py-8">
      <i className="fas fa-play text-white"></i>
    </div>
  );
};

export default VideoControls;
